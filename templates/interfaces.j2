# This file describes the network interfaces available on your system
# and how to activate them. For more information, see interfaces(5).

source /etc/network/interfaces.d/*

# The loopback network interface
auto lo
iface lo inet loopback

# The primary network interface
auto {{ primary_interface }}
iface {{ primary_interface }} inet dhcp
# The secondary network interface
auto {{ secondary_interface }}
iface {{ secondary_interface }} inet static
  address {{ secondary_interface_ip }}
  netmask {{ secondary_interface_nm }}
  gateway {{ secondary_interface_gw }}
  dns-nameservers {{ dns_server }}
  pre-up sleep 2

